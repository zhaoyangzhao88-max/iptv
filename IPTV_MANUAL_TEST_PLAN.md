# IPTV 播放器人工测试计划

> **目的**：按步骤定位“界面能打开，但按钮无反应、电视节目不显示”的真实原因。
>
> **重要原则**：本文件只用于测试、记录证据和定位问题，**不包含修复方案或代码改动设计**。一次只执行一个测试项；不要在没有记录结果前跳到后续步骤。

---

## 1. 当前已知现象

| 项目 | 当前现象 |
| --- | --- |
| 应用启动 | Electron 窗口可以打开并显示界面 |
| 页面交互 | 点击按钮后，看起来没有响应 |
| 频道数据 | 电视节目/频道列表没有显示 |
| 已验证基础服务 | 内置 Node API 的 `http://127.0.0.1:3000/health` 可返回 200 |
| 需定位的范围 | Electron 主进程、预加载桥接、渲染进程初始化、频道数据加载、DOM 渲染、按钮事件、播放器/诊断功能 |

---

## 2. 测试前准备

### 2.1 测试环境记录

每次开始新一轮测试，先填写以下信息：

| 字段 | 本次填写 |
| --- | --- |
| 测试日期/时间 |  |
| 测试人员 |  |
| 操作系统版本 |  |
| Node.js 版本（`node -v`） |  |
| npm 版本（`npm -v`） |  |
| Electron 运行方式 | `npm start` / 打包 EXE / 其他： |
| 应用版本/构建来源 |  |
| 是否联网 | 是 / 否 / 不确定 |
| 是否使用代理/VPN | 是 / 否 / 不确定 |
| 是否存在旧配置或旧数据 | 是 / 否 / 不确定 |
| Git 工作区状态 | 记录 `git status --short --untracked-files=all` 输出或截图位置 |

### 2.2 启动方式

开发模式：

```bash
npm start
```

或从根目录：

```bash
npm run start
```

打包版本：

```text
iptv-project/dist/OwlIPTV 1.0.0.exe
```

### 2.3 本轮测试证据目录

建议每轮单独保存，不要覆盖旧证据：

```text
test-evidence/
  YYYY-MM-DD-HHMM/
    startup.log
    renderer-console.txt
    main-process.log
    screenshots/
    network-results.md
    observations.md
```

> 如果暂时不建目录，也至少在本文件对应测试项中记录截图文件名、日志片段和操作结果。

---

## 3. 测试总流程

按以下顺序执行，不跳步：

1. **确认复现条件**：界面、频道、按钮的具体表现。
2. **确认启动链路**：Electron 主进程与 Node API 是否都成功启动。
3. **检查渲染进程错误**：浏览器控制台是否有初始化错误。
4. **检查预加载桥接**：页面是否拿到 `window.electronAPI` 或项目实际暴露的桥接对象。
5. **检查频道数据链路**：本地数据、远程数据、归一化数据和最终状态。
6. **检查 DOM 渲染**：频道数据是否进入页面、是否被过滤或隐藏。
7. **检查按钮事件链路**：DOM 元素、事件监听、处理函数、状态更新、视觉反馈。
8. **检查播放器/诊断链路**：频道可见后能否选择、播放、诊断或切换线路。
9. **整理结论**：精确指出首次失败发生在哪一层，不猜测修复方式。

---

## 4. 阶段 A：确认问题可稳定复现

### A-01：冷启动界面观察

**目的**：确认问题是否每次启动都出现。

**操作**：

1. 完全退出应用。
2. 确认任务管理器中没有残留 `electron.exe`、`OwlIPTV.exe` 或 Node 服务进程。
3. 使用开发模式或 EXE 启动应用。
4. 等待页面完全稳定（建议至少 10 秒）。
5. 截图保存主界面。

**记录**：

| 检查项 | 结果 | 证据/备注 |
| --- | --- | --- |
| 窗口是否正常出现 | 通过 / 失败 |  |
| 页面是否完整显示 | 通过 / 失败 |  |
| 是否显示加载状态 | 是 / 否 |  |
| 频道区域是否为空 | 是 / 否 |  |
| 是否有错误提示 | 是 / 否 |  |
| 是否有控制台或弹窗报错 | 是 / 否 |  |
| 问题是否可稳定复现 | 是 / 否 / 偶现 |  |

### A-02：按钮无响应的精确描述

**目的**：将“无反应”拆成可观察事实。

**操作**：依次点击界面中每个可点击控件一次，不要连续快速点击。

**记录表**：

| 控件名称/位置 | 点击前状态 | 点击动作 | 点击后视觉变化 | 是否有声音/弹窗/页面变化 | 是否有控制台错误 | 结果 |
| --- | --- | --- | --- | --- | --- | --- |
| 频道分类 |  | 单击 |  |  |  | 通过 / 失败 |
| 搜索框 |  | 输入关键词 |  |  |  | 通过 / 失败 |
| 频道项 |  | 单击 |  |  |  | 通过 / 失败 |
| 播放按钮 |  | 单击 |  |  |  | 通过 / 失败 |
| 切换线路按钮 |  | 单击 |  |  |  | 通过 / 失败 |
| 刷新/重新加载按钮 |  | 单击 |  |  |  | 通过 / 失败 |
| 设置按钮 |  | 单击 |  |  |  | 通过 / 失败 |
| 诊断按钮 |  | 单击 |  |  |  | 通过 / 失败 |
| 其他： |  |  |  |  |  |  |

**通过标准**：每个控件都有可预期的可见反馈，或有明确的禁用状态与原因。

---

## 5. 阶段 B：启动链路与基础服务

### B-01：确认 Node API 健康状态

**目的**：确认内置 API 是否在应用启动后可访问。

**操作**：应用运行时执行：

```bash
curl -i http://127.0.0.1:3000/health
```

**期望**：HTTP 200，JSON 中含有 `"status":"ok"`。

| 项目 | 结果 | 原始输出/截图 |
| --- | --- | --- |
| 命令可执行 | 通过 / 失败 |  |
| HTTP 状态码 |  |  |
| 响应内容 |  |  |
| 应用启动日志是否显示 API ready | 是 / 否 |  |

### B-02：检查端口和残留进程

**目的**：排除端口冲突或旧进程占用导致“窗口打开但服务不是本次启动”的情况。

**操作**：

```bash
netstat -ano | findstr :3000
```

记录 PID，再在任务管理器或命令行确认对应进程。

| 检查项 | 结果 | 备注 |
| --- | --- | --- |
| 3000 端口有监听 | 是 / 否 |  |
| 监听进程属于本次应用 | 是 / 否 / 不确定 |  |
| 存在重复 Node/Electron 进程 | 是 / 否 |  |
| 端口被其他应用占用 | 是 / 否 |  |

### B-03：主进程日志检查

**目的**：确认 Electron 主进程、预加载脚本、Node 子进程启动期间没有异常。

**操作**：保存 `npm start` 的完整终端输出；重点记录包含以下关键字的行：

```text
error
failed
exception
preload
NodeAPI
ready
port
EADDRINUSE
uncaught
```

| 检查项 | 结果 | 关键日志 |
| --- | --- | --- |
| 主进程正常启动 | 通过 / 失败 |  |
| preload 相关错误 | 有 / 无 |  |
| Node API 启动错误 | 有 / 无 |  |
| 端口冲突 | 有 / 无 |  |
| 未捕获异常 | 有 / 无 |  |

---

## 6. 阶段 C：渲染进程与浏览器控制台

### C-01：打开开发者工具并保存控制台

**目的**：确认页面脚本是否在初始化时抛错；这通常是“界面存在但所有按钮失效、频道不显示”的第一证据点。

**操作**：

1. 打开应用开发者工具（若项目菜单或快捷键提供）。
2. 切换到 **Console**。
3. 勾选 Preserve log（如有）。
4. 刷新页面或重启应用。
5. 保存所有 `error`、`warning`、未处理 Promise rejection 和 CSP 相关信息。

| 检查项 | 结果 | 错误全文/截图位置 |
| --- | --- | --- |
| Console 有红色错误 | 是 / 否 |  |
| 有 `Uncaught` / `Unhandled Promise Rejection` | 是 / 否 |  |
| 有模块加载失败 | 是 / 否 |  |
| 有 `window.electronAPI`/桥接对象不存在错误 | 是 / 否 |  |
| 有数据加载或 JSON 解析错误 | 是 / 否 |  |
| 有 DOM 元素为 null 的错误 | 是 / 否 |  |

> **记录要求**：错误必须完整复制，包括报错文本、文件名、行号和堆栈；不要只写“有报错”。

### C-02：确认页面初始化完成标志

**目的**：确定 `app.js` 的初始化流程是否开始、是否结束、卡在哪一步。

**操作**：在 Console 中观察已有日志；若页面提供全局状态对象或调试状态，记录其内容。不要修改代码。

| 检查项 | 结果 | 证据 |
| --- | --- | --- |
| 页面主脚本已加载 | 是 / 否 / 不确定 |  |
| DOMContentLoaded 已触发 | 是 / 否 / 不确定 |  |
| 初始化开始 | 是 / 否 / 不确定 |  |
| 初始化结束 | 是 / 否 / 不确定 |  |
| 初始化卡住或抛错的位置 |  |  |

### C-03：检查页面资源加载

**目的**：确认 HTML、JS、CSS、数据文件和本地 Hls.js 没有加载失败。

**操作**：开发者工具切换到 **Network**，刷新页面，按 Status 或 Failed 排序。

| 资源类别 | 是否请求 | 状态码/结果 | 失败原因 | 备注 |
| --- | --- | --- | --- | --- |
| `app.js` 与模块脚本 | 是 / 否 |  |  |  |
| CSS/静态资源 | 是 / 否 |  |  |  |
| `hls.min.js` | 是 / 否 |  |  |  |
| `channels.json` | 是 / 否 |  |  |  |
| 远程频道源 | 是 / 否 |  |  |  |
| Node API 请求 | 是 / 否 |  |  |  |

---

## 7. 阶段 D：预加载桥接与 Electron IPC

### D-01：检查桥接对象是否存在

**目的**：确认页面有权调用预加载脚本暴露的受限 API。

**操作**：在页面 Console 中输入：

```js
typeof window.electronAPI
```

如果项目实际使用的桥接对象名称不同，记录实际名称并执行对应检查。

| 检查项 | 结果 | 原始输出 |
| --- | --- | --- |
| `window.electronAPI` 存在 | 是 / 否 / 不适用 |  |
| 桥接对象的方法列表符合预期 | 是 / 否 / 不确定 |  |
| 调用桥接方法时报错 | 是 / 否 |  |
| 报错全文 |  |  |

### D-02：确认 IPC 请求与返回

**目的**：确认按钮或初始化发出的 IPC 是否到达主进程并得到返回。

**操作**：针对一个已知需要主进程支持的功能，记录：

1. 点击前 Console/Network/日志状态。
2. 点击一次。
3. 点击后渲染进程 Console 和启动终端的新日志。
4. 是否出现 IPC 相关错误、超时或返回空值。

| 功能 | 是否发出 IPC | 主进程是否收到 | 是否有返回 | 返回是否有效 | 证据 |
| --- | --- | --- | --- | --- | --- |
| 初始加载频道 | 是 / 否 / 不确定 | 是 / 否 / 不确定 | 是 / 否 / 不确定 | 是 / 否 / 不确定 |  |
| 刷新数据 | 是 / 否 / 不确定 | 是 / 否 / 不确定 | 是 / 否 / 不确定 | 是 / 否 / 不确定 |  |
| 设置/保存 | 是 / 否 / 不确定 | 是 / 否 / 不确定 | 是 / 否 / 不确定 | 是 / 否 / 不确定 |  |
| 诊断/导出 | 是 / 否 / 不确定 | 是 / 否 / 不确定 | 是 / 否 / 不确定 | 是 / 否 / 不确定 |  |

---

## 8. 阶段 E：频道数据加载链路

### E-01：确认本地频道文件存在且可读取

**目的**：区分“本地基础数据没有加载”和“加载后没有渲染”。

**操作**：检查：

```text
iptv-project/data/channels.json
```

只记录，不修改。

| 检查项 | 结果 | 备注 |
| --- | --- | --- |
| 文件存在 | 是 / 否 |  |
| 文件大小合理（非 0） | 是 / 否 |  |
| JSON 格式可读取 | 是 / 否 |  |
| 频道数量 |  |  |
| 有 `name` 字段的条目数 |  |  |
| 有 `url` 或 `urls`/`routes` 的条目数 |  |  |

### E-02：确认数据加载函数返回值

**目的**：确认加载层是否获得频道数组。

**操作**：通过现有调试输出、开发者工具或项目已有状态对象，记录以下阶段的数据量。不要自行改写业务代码。

| 数据阶段 | 预期 | 实际数量 | 是否为空 | 证据 |
| --- | --- | --- | --- | --- |
| 原始本地频道数据 | 大于 0 |  | 是 / 否 |  |
| 远程候选数据（如启用） | 可为 0，但应有明确原因 |  | 是 / 否 |  |
| 归一化后的频道数据 | 大于 0 |  | 是 / 否 |  |
| 合并后的频道数据 | 大于 0 |  | 是 / 否 |  |
| 进入应用状态的频道数据 | 大于 0 |  | 是 / 否 |  |

### E-03：确认数据是否被过滤/隐藏

**目的**：判断频道存在但没有显示，是否由筛选、隐藏状态、搜索关键词或分类状态造成。

**操作**：记录当前 UI 与状态：

| 检查项 | 当前值 | 是否符合预期 | 证据 |
| --- | --- | --- | --- |
| 搜索关键词 |  | 是 / 否 |  |
| 当前分类 |  | 是 / 否 |  |
| 是否只显示收藏 | 是 / 否 | 是 / 否 |  |
| 是否只显示健康频道 | 是 / 否 | 是 / 否 |  |
| 频道是否被 `hidden` 状态过滤 | 是 / 否 / 不确定 |  |  |
| 过滤前频道数 |  |  |  |
| 过滤后频道数 |  |  |  |

---

## 9. 阶段 F：DOM 渲染检查

### F-01：确认频道容器存在

**目的**：确认页面中承载频道列表的 DOM 容器正常存在。

**操作**：在 Elements 面板定位频道列表容器，记录其选择器、子节点数量和可见性。

| 检查项 | 结果 | 证据 |
| --- | --- | --- |
| 频道列表容器存在 | 是 / 否 |  |
| 容器可见 | 是 / 否 |  |
| 容器高度/宽度大于 0 | 是 / 否 |  |
| 容器下有频道子节点 | 是 / 否 |  |
| 子节点数量 |  |  |
| 子节点被 CSS 隐藏 | 是 / 否 / 不确定 |  |
| 有覆盖层遮挡 | 是 / 否 / 不确定 |  |

### F-02：区分“没有渲染”和“渲染但不可见”

| 现象 | 结果 | 证据 |
| --- | --- | --- |
| DOM 中完全没有频道项 | 是 / 否 |  |
| DOM 中有频道项但页面不可见 | 是 / 否 |  |
| 频道项文本为空 | 是 / 否 |  |
| 频道项有内容但颜色/透明度异常 | 是 / 否 |  |
| 频道项被滚动容器裁切 | 是 / 否 |  |
| 控制台有渲染相关异常 | 是 / 否 |  |

---

## 10. 阶段 G：按钮事件链路

### G-01：确认按钮本身不是禁用状态

**目的**：区分“事件没绑定”和“按钮被业务状态故意禁用”。

| 按钮 | DOM 存在 | `disabled` | CSS 是否拦截点击 | 是否被遮挡 | 备注 |
| --- | --- | --- | --- | --- | --- |
| 刷新 | 是 / 否 | 是 / 否 | 是 / 否 / 不确定 | 是 / 否 / 不确定 |  |
| 设置 | 是 / 否 | 是 / 否 | 是 / 否 / 不确定 | 是 / 否 / 不确定 |  |
| 诊断 | 是 / 否 | 是 / 否 | 是 / 否 / 不确定 | 是 / 否 / 不确定 |  |
| 播放/频道项 | 是 / 否 | 是 / 否 | 是 / 否 / 不确定 | 是 / 否 / 不确定 |  |

### G-02：检查单个按钮点击后的完整链路

**目的**：对每个按钮分别确认“点击事件 → 处理函数 → 状态变化 → UI 更新”哪一步首次断开。

**每个按钮都按以下表格填写一次：**

| 步骤 | 结果 | 证据 |
| --- | --- | --- |
| 1. 鼠标点击是否命中目标元素 | 通过 / 失败 / 不确定 |  |
| 2. click 事件是否触发 | 通过 / 失败 / 不确定 |  |
| 3. 事件处理函数是否开始执行 | 通过 / 失败 / 不确定 |  |
| 4. 处理函数是否抛错 | 是 / 否 / 不确定 |  |
| 5. 是否调用 IPC / 数据函数 / 状态函数 | 是 / 否 / 不确定 |  |
| 6. 调用是否返回 | 是 / 否 / 不确定 |  |
| 7. 状态是否变化 | 是 / 否 / 不确定 |  |
| 8. DOM 是否重新渲染 | 是 / 否 / 不确定 |  |
| 9. 用户可见反馈是否出现 | 是 / 否 |  |

### G-03：全局事件/遮罩检查

**目的**：确认没有透明遮罩、错误的 `pointer-events`、modal 层或全局异常阻止所有点击。

| 检查项 | 结果 | 证据 |
| --- | --- | --- |
| 页面上存在全屏遮罩 | 是 / 否 / 不确定 |  |
| 遮罩 `z-index` 高于按钮 | 是 / 否 / 不确定 |  |
| `pointer-events: none` 影响按钮 | 是 / 否 / 不确定 |  |
| `pointer-events: auto` 的遮罩截获点击 | 是 / 否 / 不确定 |  |
| Console 中出现全局运行时异常 | 是 / 否 |  |
| 点击任意空白区域是否也无反应 | 是 / 否 |  |

---

## 11. 阶段 H：频道选择、播放与线路切换

> 前提：必须先在阶段 E/F 确认至少有一个频道能显示。若频道仍为 0，先停止本阶段并记录阻塞点。

### H-01：频道选择

| 检查项 | 结果 | 证据 |
| --- | --- | --- |
| 点击频道后选中样式变化 | 通过 / 失败 |  |
| 当前频道名称变化 | 通过 / 失败 |  |
| 播放区域状态变化 | 通过 / 失败 |  |
| Console 无新错误 | 通过 / 失败 |  |

### H-02：播放请求

| 检查项 | 结果 | 证据 |
| --- | --- | --- |
| video 元素存在 | 是 / 否 |  |
| 选择频道后 video `src` 或 Hls 实例变化 | 是 / 否 / 不确定 |  |
| Network 出现媒体/playlist 请求 | 是 / 否 |  |
| 请求状态码 |  |  |
| 播放器出现可理解的加载/失败反馈 | 是 / 否 |  |
| Console 有 Hls/video error | 是 / 否 |  |

### H-03：备用线路

| 检查项 | 结果 | 证据 |
| --- | --- | --- |
| 测试频道有多条线路 | 是 / 否 |  |
| 主线路失败后尝试备用线路 | 是 / 否 / 不确定 |  |
| 成功线路被标记/记录 | 是 / 否 / 不确定 |  |
| 全部失败时频道仍保持可见 | 是 / 否 |  |
| 可手动再次尝试或切换 | 是 / 否 |  |

---

## 12. 阶段 I：数据源与网络差异测试

### I-01：离线/网络不可用表现

**目的**：确认网络问题时，本地稳定频道是否仍显示，并得到可理解反馈。

| 场景 | 操作 | 频道是否仍显示 | 是否出现明确反馈 | Console/日志错误 | 结果 |
| --- | --- | --- | --- | --- | --- |
| 正常联网 |  |  |  |  |  |
| 断网后启动 | 断开网络后重启应用 |  |  |  |  |
| 网络恢复后刷新 | 恢复网络后点击刷新 |  |  |  |  |
| 代理/VPN 开启 |  |  |  |  |  |
| 代理/VPN 关闭 |  |  |  |  |  |

### I-02：开发模式与打包版对比

| 项目 | 开发模式 `npm start` | 打包 EXE | 是否一致 | 证据 |
| --- | --- | --- | --- | --- |
| 界面打开 |  |  | 是 / 否 |  |
| 频道显示 |  |  | 是 / 否 |  |
| 按钮可点击 |  |  | 是 / 否 |  |
| Console/日志错误 |  |  | 是 / 否 |  |
| Node API health |  |  | 是 / 否 |  |

---

## 13. 问题定位记录单

每发现一个问题，单独复制本节填写一份。不要把多个问题混在同一个记录单里。

### 缺陷编号：BUG-____

| 字段 | 内容 |
| --- | --- |
| 发现时间 |  |
| 测试版本/运行方式 |  |
| 复现概率 | 每次 / 偶现 / 一次 |
| 前置条件 |  |
| 最小复现步骤 | 1.  2.  3.  |
| 实际结果 |  |
| 预期结果 |  |
| 首次失败层级 | 主进程 / preload / 渲染初始化 / 数据加载 / 状态过滤 / DOM 渲染 / 事件监听 / IPC / 播放器 / 网络 / 不确定 |
| Console 错误全文 |  |
| 主进程日志片段 |  |
| Network 请求信息 |  |
| 截图/录屏文件名 |  |
| 是否影响所有按钮 | 是 / 否 |
| 是否影响所有频道 | 是 / 否 |
| 是否阻塞后续测试 | 是 / 否 |
| 初步事实结论（只写观察到的事实） |  |

---

## 14. 每轮测试结束清单

在宣布某一轮结束前逐项确认：

- [ ] 已记录运行方式、时间和环境。
- [ ] 已保存 Electron 主进程启动日志。
- [ ] 已保存渲染进程 Console 错误全文或确认无错误。
- [ ] 已确认 Node API `/health` 的实际响应。
- [ ] 已记录本地频道文件是否存在、数据条数与格式状态。
- [ ] 已区分“数据为空”“数据被过滤”“数据未渲染”“渲染后不可见”。
- [ ] 已对至少一个按钮完成“点击 → 事件 → 处理 → 状态 → UI”的链路记录。
- [ ] 已记录开发模式和打包版是否存在差异。
- [ ] 已附上截图、日志文件或完整命令输出的位置。
- [ ] 已写明首次失败发生在哪一层；若无法判断，明确写“不确定”及缺少的证据。
- [ ] 本轮只测试和记录，未在未确认根因前修改业务代码。

---

## 15. 建议测试顺序（最省时间）

如果你只想先用最少步骤找到大方向，按这个顺序执行：

1. A-01：冷启动截图，确认频道区域与按钮表现。
2. B-01：确认 `/health` 返回 200。
3. C-01：查看 Console 红色错误并保存全文。
4. C-03：检查 `app.js`、模块、`channels.json`、`hls.min.js` 有没有加载失败。
5. D-01：检查桥接对象是否存在。
6. E-01、E-02：确认本地频道文件和加载后数组数量。
7. E-03、F-01、F-02：区分“有数据但被过滤/不可见”和“根本没渲染”。
8. G-02：选择一个按钮，完整追踪点击链路。
9. 只有频道显示后，再执行 H-01 到 H-03 的播放和备用线路测试。
10. 最后执行 I-02，对比开发模式与打包 EXE。

---

## 17. 修复方案（通过测试证据后执行）

> **前置条件**：本节是“测试确认根因后”的实施路线，不是立即修改代码的清单。必须先完成第 15 节的优先测试顺序，并在第 13 节记录第一个确定失败点。一次只处理一个已确认根因。

### 17.1 修复总原则

1. **先证明，再修改**：不得因为“看起来像数据路径问题”就直接修改 preload、频道路径、过滤器或事件代码。
2. **最小改动**：只修改证据显示的第一个失败边界，不顺手重构其他模块。
3. **用户可见状态**：初始化失败、数据源失败和真实空频道必须有不同且可理解的页面反馈，不能只留下空白区域。
4. **保留安全规则**：不得为让频道显示而直接绕过 hidden、failed、备用线路、multicast 或 URL 安全校验。
5. **先回归、后宣布**：每项修复都需要对应的复现测试/真实 Electron 冒烟验证和全量回归。

### 17.2 根因 → 修复动作对照表

| 测试确认的第一个失败点 | 允许采取的最小修复动作 | 关键文件 | 必须新增/更新的验证 |
| --- | --- | --- | --- |
| `init()` 在缓存 DOM、绑定事件、加载数据或首次渲染阶段抛异常 | 为初始化加入统一错误边界、阶段标识和可见错误状态；保留开发者日志与原始错误 | `app/app.js`、必要时 `app/index.html` | 真实 Electron 启动后应显示频道、空状态或明确错误状态；Console 不再出现未处理 rejection |
| Electron/preload 桥接对象不存在，或 `readFile`/`getAppPath`/`pathJoin` 调用失败 | 只修正被证实缺失的 preload 暴露、IPC handler 或参数契约 | `preload.js`、`main.js`、`app/modules/dataLoader.js` | 记录桥接对象方法、实际数据路径和读文件成功结果；开发版与打包版均验证 |
| `data/channels.json` 路径与实际开发/打包资源布局不一致 | 只修正实际错误的路径计算或打包 `files` 配置 | `dataLoader.js`、`preload.js`、`package.json` | 路径契约测试；开发版与 `app.asar`/打包版都能读取频道文件 |
| 文件/API 读取失败后被静默当成空数组 | 区分“加载失败”和“确实零频道”；返回可识别失败结果并显示错误状态 | `dataLoader.js`、`app.js` | 数据读取失败时页面显示明确错误，而不是空白；已有成功加载行为不变 |
| 原始数据有频道，但归一化后为 0 | 统计并定位 hidden、failed、failure-limit、无有效线路或 schema/key 不匹配；只修正确认的字段/键兼容 | `dataLoader.js`、相关 state/override 模块 | 新增最小数据样本回归；确认保护规则仍有效 |
| 归一化后有频道，但分类/搜索/设置/隐藏过滤后为 0 | 检查并只修正确认错误的本地 override、分类、搜索或 `showMulticast` 状态处理；确认 reset/settings 可以恢复可见频道 | `virtualGrid.js`、设置/状态模块、`inputHandler.js` | 过滤前后数量断言；真实窗口点击 reset/settings 后列表反馈正确 |
| `currentChannels > 0`，但 DOM 没有频道卡 | 检查并只修正虚拟列表容器尺寸、范围计算、spacer/card append 或 CSS 裁切问题 | `virtualGrid.js`、相关 CSS/`index.html` | 断言至少一张 `.channel-card` 可见；容器高度和可见范围有有效值 |
| 所有按钮无响应，且初始化在 `bindEvents()` 前/中失败 | 修复实际异常；确认事件绑定在初始化完成前后顺序正确 | `app/app.js`、`app/modules/inputHandler.js` | 对分类、设置/刷新、诊断至少各点击一次并观察状态/UI 反馈 |
| 仅诊断、设置、播放等少数按钮无响应 | 修复对应的 dynamic import、选择器或模块加载失败，不修改其他按钮链路 | `inputHandler.js`、相应 `diagnostic.js`/`settings.js`/`player.js` | 针对失败按钮新增模块加载/真实点击回归 |
| 仅最小化、关闭等窗口控件无响应 | 修复实际缺失的 preload IPC 暴露或主进程 handler | `preload.js`、`main.js` | 真实窗口验证对应 IPC 往返；不影响业务按钮 |
| 元素可见但 click 没命中 | 修复已证实的遮罩、`pointer-events`、disabled 或 z-index 问题 | `index.html`、CSS 文件 | Elements/Computed 验证命中目标元素；点击后有可见反馈 |

### 17.3 初始化失败的统一处理标准

若测试确认初始化异常，修复应满足：

- 页面显示简洁的失败原因类别，例如“频道加载失败”“页面初始化失败”或“无法读取本地频道数据”。
- 为用户提供重试或重新加载入口；不展示 token、私有 URL、完整错误堆栈或敏感参数。
- Console/日志保留完整阶段信息，便于开发排查。
- “真正没有可用频道”显示现有空状态；“加载失败”显示错误状态；两者不可混淆。
- 任何异常都不能让页面停在没有频道、没有空状态、没有错误说明的空白状态。

### 17.4 真实 Electron 冒烟测试补齐方案

现有测试大量使用 VM、模拟 DOM 或模块级测试，不能替代真实窗口验证。修复过程中需要补充真正的 Electron 冒烟测试，至少覆盖：

1. 启动 Electron/BrowserWindow，等待页面初始化完成。
2. 捕获 renderer Console、page error、未处理 Promise rejection 和主进程异常。
3. 断言页面出现以下三种状态之一：至少一个 `.channel-card`、明确 `.empty-state`、明确初始化/数据加载错误状态。
4. 点击至少一个分类、一个 reset/settings 类控件和一个诊断/操作类控件，确认存在状态或可见 UI 反馈。
5. 分别验证 `npm start` 开发模式与打包资源布局；若只支持一方，必须在测试结论中明确记录。
6. 将首次复现的真实问题加入回归，避免只修复手工环境中的偶发现象。

### 17.5 每项修复后的验收命令

每次只处理一个根因，完成后执行并保存完整输出：

```bash
node --check iptv-project/app/checker-worker.js
npm run test:python
npm run test:node
npm run test:player
npm test
git diff --check
git status --short --untracked-files=all
```

同时必须：

- [ ] 实际启动 `npm start`，记录频道卡片/空状态/错误状态和代表性按钮反馈。
- [ ] 实际运行打包版 `iptv-project/dist/OwlIPTV 1.0.0.exe`（若本轮改动影响打包/路径/preload）。
- [ ] 保存 Console、主进程日志和截图。
- [ ] 确认 `channels.json`、pytest 日志和其他用户已有改动未被覆盖。
- [ ] 确认没有新增 `uv.lock`、未授权生成物或敏感数据日志。
- [ ] 在第 13 节缺陷记录单中填写修复前证据、修复后证据与对应回归测试。

---

## 18. 当前测试结论（待填写）

| 项目 | 当前结论 | 证据编号 |
| --- | --- | --- |
| 应用窗口能否打开 | 待测试 |  |
| Node API 是否可用 | 待测试 |  |
| 渲染进程是否有初始化错误 | 待测试 |  |
| 预加载桥接是否可用 | 待测试 |  |
| 本地频道数据是否加载 | 待测试 |  |
| 频道是否被过滤或隐藏 | 待测试 |  |
| 频道 DOM 是否生成 | 待测试 |  |
| 按钮点击事件是否触发 | 待测试 |  |
| IPC 是否正常往返 | 待测试 |  |
| 播放器是否收到频道线路 | 待测试 |  |
| 首个已确认失败点 | 待测试 |  |

---

## 19. 备注

- 不要因为“测试脚本通过”就跳过人工界面测试；自动化测试和真实 Electron 窗口的运行环境不同。
- 不要因为界面能显示，就假设渲染逻辑完整执行；一个初始化异常可能导致 HTML 存在但所有交互未绑定。
- 不要在没有保存 Console、主进程日志和网络证据前直接改代码。
- 如发现敏感 URL、token、账号、Cookie 或私有地址，记录时必须脱敏，不要把完整内容写入测试记录或截图文件名。
