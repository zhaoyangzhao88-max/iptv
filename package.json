{
  "name": "iptv-monorepo",
  "version": "1.0.0",
  "description": "IPTV all-stack monorepo: data engine (Python/Node) + Electron player",
  "private": true,
  "scripts": {
    "start": "cd iptv-project && npm start",
    "dist": "cd iptv-project && npm run dist",
    "test": "npm run test:node && npm run test:python && npm run test:player",
    "test:python": "node scripts/run-python-tests.cjs",
    "test:node": "cd iptv-engine-b/node_api && node --test tests/*.test.js",
    "test:player": "npm --prefix iptv-project test"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
